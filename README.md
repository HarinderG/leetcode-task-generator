# leetcode-task-generator
The goal of this script is to fetch a random unsolved LeetCode problems, and integrate it with Todoist.

## Notes
- Run `$ pip3 install -r requirements.txt` to install dependancies.
